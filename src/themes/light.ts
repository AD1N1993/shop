import {ThemeProps} from "./dark";

const theme: ThemeProps = {
    id: "light",
    primaryColor: '#f8049c',
    secondaryColor: '#fdd54f',
    bodyBackgroundColor: "#fff",
    bodyFontColor: "#000"
}

export default theme